import { FormContainer } from "./styles";
import Input from "../Input";
import Button from "../Button";

const Form = () => {
  return (
    <FormContainer>
      <Input />
      <Button />
    </FormContainer>
  );
};

export default Form;
